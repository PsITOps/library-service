var expect = require('chai').expect;
var auth = require('./auth');

describe('Auth', () => {

    beforeEach(function () {

    })

    it('should adds 1 and 2 with the result of 3', () => {

        expect(auth.add(1, 2)).to.be.equal(3);

    })


    it('should register a new user');
})